# SmartElderCare v1.0.0 封版说明

## 启动前配置

复制 `.env.example` 的变量到部署环境。以下变量为必填项：

- `DB_USERNAME`、`DB_PASSWORD`
- `JWT_SECRET`，长度至少 32 个字符
- `AI_ENABLED=true` 与 `DASHSCOPE_API_KEY`，启用 AI 问答时必填
- `OSS_ENDPOINT`、`OSS_ACCESS_KEY_ID`、`OSS_ACCESS_KEY_SECRET`、`OSS_BUCKET_NAME`，使用文件上传时必填

`application.yml` 只保留非敏感的默认运行参数，所有凭证均从环境变量读取。

## 构建命令

```powershell
mvn compile -DskipTests
cd ui/ui-admin; npm run build
cd ../ui-app; npm run build
```

## 封版边界

本版本覆盖老人、家属、床位、护理计划/任务、体检预约/执行/报告、权限与 AI 问答。管理员首页统计由数据库实时汇总，不使用演示图表数据。
