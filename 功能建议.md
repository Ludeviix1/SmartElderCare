# 养老管理系统 - 功能扩展建议

> 基于现有项目（Spring Boot 3.4.5 + Vue 3 + MyBatis-Plus + MySQL）的分析，以下是可添加的功能建议。

---

## 一、核心业务增强

### 1. 护理计划周期性任务生成
- **现状**：`CarePlanServiceImpl` 仅按计划开始日期生成一次护理任务，未根据 `execute_cycle`（天/周/月）和 `execute_frequency` 生成多次任务
- **建议**：根据执行周期和频率，在计划时间段内批量生成所有待执行任务，支持按日/周/月循环

### 2. 老人健康档案管理
- 新增 `health_record` 表，记录老人的体温、血压、心率、血糖等每日体征数据
- 支持护工每日打卡时录入健康数据
- 提供健康趋势图表（折线图/柱状图）

### 3. 老人入住/退住流程管理
- **现状**：`elder.status` 字段有多种状态（入住中/请假/退住中/已退住），但无流程驱动
- **建议**：
  - 新增入住登记表单（床位分配、紧急联系人、入住协议）
  - 退住审批流程（申请→审核→结算→完成）
  - 请假登记与归院确认

### 4. 床位管理
- 新增 `bed`、`building`、`room` 实体，构建 楼栋→房间→床位 的三级结构
- 老人入住时分配床位，退住时释放
- 前端展示床位占用情况（可用/已占用/维护中）

### 5. 费用管理
- 新增 `fee_record` 表，关联老人和护理计划
- 按护理等级 + 护理项目自动计算月度费用
- 支持费用账单生成与导出（Excel/PDF）
- 支持缴费记录和欠费提醒

---

## 二、系统安全与权限

### 6. JWT 密钥与 OSS 凭证外部化
- **现状**：`JwtUtil` 密钥为硬编码 `"gao"`，`AliOSSUtil` 中 AccessKey/Secret 硬编码在源码中
- **建议**：将密钥配置移至 `application.yml` 或环境变量，避免泄露风险

### 7. 接口限流
- 基于 Redis + 滑动窗口/令牌桶实现接口限流
- 防止暴力登录、频繁导出等滥用行为

### 8. 操作日志审计
- 新增 `operation_log` 表，记录关键操作（增删改、登录、导出等）
- 使用 AOP 切面自动拦截记录
- 前端提供操作日志查询页面

### 9. 密码安全增强
- **现状**：密码使用明文存储和比对
- **建议**：使用 BCrypt/SM3 加密存储，支持密码强度策略（长度、复杂度）

---

## 三、护理任务优化

### 10. 护理任务提醒通知
- 任务到期前通过站内消息/短信/微信推送提醒护工
- 支持消息已读/未读状态管理
- 新增 `notification` 表

### 11. 护理任务执行超时预警
- 任务超过计划执行时间未完成时，标记为"超时"并高亮显示
- 支持管理员查看超时任务统计

### 12. 护理交接班管理
- 新增交接班记录表，记录每班次护理情况
- 交班时自动生成待办事项汇总
- 支持交接班签字确认

### 13. 护理质量评估
- 新增护理评价表，对每次护理执行打分
- 统计护工护理完成率、及时率、好评率
- 按月生成护理质量报告

---

## 四、数据统计与报表

### 14. 数据大屏/仪表盘
- 首页 Dashboard 展示：
  - 今日待执行任务数 / 已完成数 / 超时数
  - 在住老人总数 / 请假数 / 今日新入住
  - 护理计划总数 / 执行中 / 已结束
  - 各护理等级老人分布（饼图）
  - 月度费用收入趋势（折线图）

### 15. 老人生命周期报表
- 入住率趋势分析
- 老人年龄分布、性别分布
- 标签分布统计（独居/空巢等占比）

### 16. 护理工单统计
- 按护工维度统计任务完成量
- 按护理项目维度统计执行次数
- 按时间段导出 Excel 报表

---

## 五、前端体验优化

### 17. 动态路由权限优化
- **现状**：前端 `Index.vue` 有硬编码的默认菜单作为 fallback
- **建议**：完全依赖后端 `routerList` 动态生成菜单，移除前端硬编码菜单

### 18. 暗黑模式/主题切换
- Element Plus 支持暗黑模式（`dark` class）
- 前端添加主题切换按钮，支持亮色/暗色主题

### 19. 多语言支持
- 使用 `vue-i18n` 支持中英文切换
- 适配国际化场景

### 20. 打印功能
- 护理计划、费用账单、入住协议等支持打印
- 使用 `vue-print-nb` 或 `html2canvas` 实现

### 21. 表格功能增强
- 列拖拽排序
- 列显隐控制（用户自定义可见列）
- 表格数据本地缓存（刷新后保持筛选/排序状态）

---

## 六、运维与工程化

### 22. Swagger/OpenAPI 接口文档
- 引入 `springdoc-openapi`，自动生成 API 文档
- 前后端联调更方便

### 23. 统一返回值与异常处理增强
- **现状**：已有 `Result` 统一返回和 `GlobalExceptionHandler`
- **建议**：增加错误码枚举（`ErrorCode`），规范错误返回

### 24. 单元测试与集成测试
- **现状**：仅有 Spring Boot Test 依赖，无实际测试代码
- **建议**：为核心 Service 编写单元测试，使用 H2 内存数据库做集成测试

### 25. CI/CD 配置
- 添加 GitHub Actions / Jenkins 流水线
- 自动构建、测试、部署

### 26. Docker 容器化部署
- 编写 `Dockerfile` 和 `docker-compose.yml`
- 一键启动 MySQL + 后端 + Nginx 前端

### 27. Redis 缓存
- 引入 Redis 缓存热点数据（权限树、护理等级等）
- 用于 Session 管理、接口限流、消息队列

---

## 七、高级功能

### 28. 家属端（小程序/Web）
- 家属注册绑定老人，查看：
  - 老人基本信息和健康档案
  - 护理计划和执行记录
  - 费用账单
  - 护工联系方式
- 家属留言/视频通话预约

### 29. AI 智能推荐
- 根据老人健康数据，AI 推荐护理等级调整建议
- 根据护工技能和工作量，智能分配护理任务

### 30. IoT 设备对接
- 对接智能手环/床垫/摄像头等设备
- 实时采集老人心率、位置、睡眠数据
- 异常数据自动告警

---

## 优先级建议

| 优先级 | 功能 | 理由 |
|--------|------|------|
| **P0 - 高** | #1 护理计划周期任务生成 | 现有核心逻辑缺陷 |
| **P0 - 高** | #6 密钥凭证外部化 | 安全隐患 |
| **P0 - 高** | #9 密码加密存储 | 安全隐患 |
| **P1 - 中** | #14 数据仪表盘 | 用户体验核心 |
| **P1 - 中** | #22 Swagger 接口文档 | 开发效率 |
| **P1 - 中** | #8 操作日志审计 | 合规要求 |
| **P1 - 中** | #10 护理任务提醒 | 业务刚需 |
| **P2 - 低** | #2 健康档案 | 功能完善 |
| **P2 - 低** | #4 床位管理 | 功能完善 |
| **P2 - 低** | #5 费用管理 | 商业化需要 |
| **P3 - 远期** | #28 家属端 | 产品扩展 |
| **P3 - 远期** | #30 IoT 对接 | 智慧养老方向 |
